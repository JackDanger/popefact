<h1>Listing Pairs</h1>

<table>
  <tr>
    <th>POPE</th>
    <th>FACT</th>
  </tr>

<% @pairs.each do |pair| %>
  <tr>
    <td><%=h pair.pope.text %></td>
    <td><%=h pair.fact.text %></td>
    <td><%= link_to 'Show', pair %></td>
    <td><%= link_to 'Edit', edit_pair_path(pair) %></td>
    <td><%= link_to 'Destroy', pair, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New pair', new_pair_path %>